// components/constants/types.h
#pragma once

#include <vector>
#include <array>
#include "config.h"

using Window = std::vector<std::vector<int>>;
using MAVFeature = std::array<float, NUM_FEATURES>;
